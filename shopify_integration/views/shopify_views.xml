<odoo>
    <!-- Tree View -->
    <record id="view_shopify_integration_tree" model="ir.ui.view">
        <field name="name">shopify.integration.tree</field>
        <field name="model">shopify.integration</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'connected'" decoration-muted="state == 'draft'">
                <field name="name"/>
                <field name="shop_url"/>
                <field name="api_version"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'connected'"
                       decoration-muted="state == 'draft'"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_shopify_integration_form" model="ir.ui.view">
        <field name="name">shopify.integration.form</field>
        <field name="model">shopify.integration</field>
        <field name="arch" type="xml">
            <form string="Shopify Integration">
                <header>
                    <!-- <button name="action_connect_shopify" type="object"
                            string="Connect Shopify" class="btn-primary"
                            icon="fa-link"
                            invisible="state == 'connected'"/> -->
                    
                    <button name="test_shopify_connection" type="object"
                            string="Test Connection" class="btn-secondary"
                            icon="fa-plug"/>
                    
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,connected"/>

                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_shopify_orders"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-shopping-cart">
                            <field name="shopify_order_count"
                                widget="statinfo"
                                string="Shopify Orders"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Connected"
                            bg_color="text-bg-success"
                            invisible="state != 'connected'"/>

                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. My Shopify Store"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Store Details">
                            <field name="shop_url" placeholder="https://your-store.myshopify.com" widget="url"/>
                            <field name="api_version" placeholder="e.g. 2024-10"/>
                            <field name="company_id"/>
                        </group>
                        <group string="API Credentials">
                            <field name="api_key"/>
                            <field name="password" password="True"
                                   placeholder="API secret / access token"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Sync Actions" name="sync_actions"
                              invisible="1">
                            <div class="d-flex gap-2 mt-2">
                                <button name="fetch_products" type="object"
                                        string="Fetch Products" class="btn-primary"
                                        icon="fa-cubes"/>
                                <button name="fetch_customers" type="object"
                                        string="Fetch Customers" class="btn-primary"
                                        icon="fa-users"/>
                                <button name="fetch_orders" type="object"
                                        string="Fetch Orders" class="btn-primary"
                                        icon="fa-shopping-cart"/>
                            </div>
                            <p class="text-muted mt-3">
                                Use these actions to manually pull the latest data
                                from your connected Shopify store into Odoo.
                            </p>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_shopify_integration" model="ir.actions.act_window">
        <field name="name">Shopify Integration</field>
        <field name="res_model">shopify.integration</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_shopify_integration_root" name="Shopify Integration" parent="sale.menu_sale_config" sequence="10"/>
    <menuitem id="menu_shopify_integration" parent="menu_shopify_integration_root"
              action="action_shopify_integration" name="Shopify" sequence="1"/>
</odoo>